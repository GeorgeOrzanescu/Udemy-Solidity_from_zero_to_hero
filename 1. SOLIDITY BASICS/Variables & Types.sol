pragma solidity >=0.7.0 < 0.9.0;

// VARIABLES AND TYPES

    // 1. Booleans --> bool: The possible values are constants true and false.
    
    // 2. Integers --> int / uint: Signed and unsigned integers of various sizes.
    // !! uint and int are aliases for uint256 and int256, respectively.
    
    // 3. Strings --> are made up of ordered sequences of chars;
    
    
    contract LearnVariablesTypes{
        uint number;
        uint number2 = 10;
        bool lie = true;
        
        string errorMessage = "Not correct address";
        
        
    }